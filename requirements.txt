# ============================================================
# messenger_allInOne  필수 의존성
# ============================================================
# 설치 명령:
#   pip install -r requirements.txt
#
# Python 3.9 이상 권장 (3.11+ 최적)
# Windows 전용 프로그램입니다.
# ============================================================

# ── UI 프레임워크 ──────────────────────────────────────────────
ttkbootstrap>=1.10.1        # 테마 Tkinter 위젯 (전체 UI)

# ── 텔레그램 API ──────────────────────────────────────────────
telethon>=1.36.0            # Telegram MTProto 클라이언트
                            # (그룹 참여, 메시지 발송, 계정 관리)

# ── 이미지 처리 ───────────────────────────────────────────────
Pillow>=10.0.0              # 채팅 말풍선 PNG 생성, 스크린샷 저장

# ── 엑셀 파일 입출력 ──────────────────────────────────────────
openpyxl>=3.1.0             # .xlsx 대상 목록 읽기

# ── 클립보드 ──────────────────────────────────────────────────
pyperclip>=1.8.2            # 텍스트 클립보드 복사 (메시지 입력)

# ── HTTP 통신 ─────────────────────────────────────────────────
requests>=2.31.0            # 자동 업데이트 버전 체크

# ── Windows 전용 시스템 라이브러리 ───────────────────────────
pywin32>=306                # win32clipboard (이미지 클립보드)
                            # win32gui (창 핸들 감지)
                            # win32api (Windows API 호출)
