# ============================================================
#  messenger_allInOne  v1.72  — 필수 의존성
# ============================================================
#
#  설치 방법 (Windows 명령 프롬프트):
#    python -m pip install -r requirements.txt
#
#  또는 동봉된 install.bat 을 더블클릭하면 자동 설치됩니다.
#
#  Python 3.9 이상 권장 (3.11 + 최적, 3.14 은 아직 비권장)
#  Windows 전용 프로그램입니다.
# ============================================================

# ── UI 프레임워크 ──────────────────────────────────────────────
ttkbootstrap>=1.10.1        # 테마 Tkinter 위젯 (전체 UI 기반)

# ── 텔레그램 API (MTProto) ────────────────────────────────────
telethon>=1.36.0            # Telegram API 클라이언트
                            # (그룹 가입, 메시지 발송, 계정 관리)
                            # asyncio 기반 — 최대 15계정 동시 실행

# ── 이미지 처리 ───────────────────────────────────────────────
Pillow>=10.0.0              # 채팅 말풍선 PNG 생성, 스크린샷 저장

# ── 엑셀 파일 입출력 ──────────────────────────────────────────
openpyxl>=3.1.0             # .xlsx 대상 목록 읽기/저장

# ── 클립보드 유틸리티 ─────────────────────────────────────────
pyperclip>=1.8.2            # 텍스트 클립보드 복사 (메시지 입력)

# ── HTTP 통신 ─────────────────────────────────────────────────
requests>=2.31.0            # 자동 업데이트 버전 체크, API 호출

# ── Windows 전용 시스템 라이브러리 ───────────────────────────
pywin32>=306                # win32clipboard — 이미지 클립보드 붙여넣기
                            # win32gui      — 창 핸들 감지
                            # win32api      — Windows API 직접 호출
